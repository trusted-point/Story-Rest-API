# from fastapi import APIRouter, HTTPException
# from app.models.error_models import ErrorResponseModel
# from src.calls.sync_calls import request

# upgrade_router = APIRouter(prefix="/cosmos/gov/v1", tags=["Governance"], responses={500: {"model": ErrorResponseModel, "description": "Server-side error"}})

